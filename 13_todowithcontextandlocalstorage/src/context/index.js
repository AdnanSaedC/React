// the goal is to export everything (all the things related to context )from here
import { todoContext,TodoContextProvider,useTodoContext } from "./TodoContext";
export { todoContext,TodoContextProvider,useTodoContext } from "./TodoContext";

   